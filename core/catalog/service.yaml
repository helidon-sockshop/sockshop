kind: Service
apiVersion: v1
metadata:
  name: catalogue  # for compatibility with the existing front-end implementation
  labels:
    app: catalog
spec:
  selector:
    app: catalog
  ports:
    - port: 80
      targetPort: 7001
      name: http
